enum AdminSidebarMenu {
  overview,
  orders,
  customers,
  branches,
  categories,
  menu,
  deals,
  riders,
  banners,
  stats,
  billing,
  admins,
  settings,
}

enum OrderStatus {
  pending,
  confirmed,
  shipped,
  delivered,
  canceled,
  returned,
}

enum RiderBottomMenu { home, profile }

enum CustomerBottomMenu { home, search, cart, profile }
